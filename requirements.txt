# Зависимости AutoSkrin (Python 3.10+)
# Установка: pip install -r requirements.txt

PyQt6>=6.5          # графический интерфейс, системный трей, селектор области
mss>=9.0            # быстрый захват экрана (в т.ч. нескольких мониторов)
numpy>=1.24         # попиксельное сравнение кадров
Pillow>=10.0        # сохранение скриншотов в PNG/JPG
python-docx>=1.1    # запись скриншотов в документ Word (.docx)
pywin32>=306; sys_platform == "win32"   # вставка снимков в документ, открытый в Word

# Глобальная горячая клавиша (Ctrl+Alt+S): в Windows используется системный API
# RegisterHotKey (без доп. библиотек), на Linux/macOS — pynput.
pynput>=1.7.6; sys_platform != "win32"
